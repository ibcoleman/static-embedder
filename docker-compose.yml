services:
  postgres:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_USER: embedder
      POSTGRES_PASSWORD: embedder
      POSTGRES_DB: embeddings
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: {}
