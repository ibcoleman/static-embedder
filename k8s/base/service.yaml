apiVersion: v1
kind: Service
metadata:
  name: static-embedder
  labels:
    app.kubernetes.io/name: static-embedder
spec:
  selector:
    app.kubernetes.io/name: static-embedder
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP
